# Changelog

## M3.1b

- Added `GET /discovery/history`.
- Added pagination with `limit` and `offset`.
- Added newest-first ordering by `created_at` and `id`.
- Added discovery-history response schemas.
- Added repository read and count methods.
- Added runtime installer, rollback, and validator.