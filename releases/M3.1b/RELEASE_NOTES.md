# M3.1b â€” Discovery History API

Adds a paginated, newest-first discovery history endpoint:

`GET /discovery/history?limit=20&offset=0`

The endpoint returns persisted provider-level discovery execution telemetry and a total row count.