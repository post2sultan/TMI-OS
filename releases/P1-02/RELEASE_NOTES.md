# TMI OS P1-02 — Evidence Extraction

Persists structured extraction results directly on campaign source documents.

Stored evidence includes source URL and type, title, extracted text,
publication and retrieval dates, language, publisher/brand, campaign
association, media assets, extraction method, and extraction status.

The campaign extraction endpoint updates the matching source document in the
same transaction while retaining the existing campaign fields for compatibility.
